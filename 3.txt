// test case #1
actor A(5){
       knownactors{
            A a;
       }
       actorvars{
            int i;
            boolean odd;
       }
       msghandler initial(int i2){
            x = a++ ? a*2 ? x-- : e : (c ? ("Bruh") : x + --w);
       }

}

main {
    A a():();
    B b(b1):("SkrattarDuForlorarDu", 2, -2);
}